---
description: Map repository structure and conventions for a task (read-only)
agent: swe
---

Explore the repository for:

$ARGUMENTS

Use `@repo-explorer` and skill `repository-mapping`. Do not modify files.

Return: architecture summary, relevant files, entry points, testing strategy, risks.
