---
description: Implement a feature with exploration, plan, tests, and verification
agent: swe
---

Feature request:

$ARGUMENTS

1. If ambiguous, load `acceptance-criteria`.
2. Assign risk tier; explore (`@repo-explorer` if unfamiliar).
3. Plan minimal changes (`@planner` if multi-file / ambiguous).
4. Implement with the right domain skill: `frontend-change` | `backend-change` | `fullstack-change` | `api-change` | `database-change` | `feature-implementation`.
5. Add/adjust tests via `@test-engineer` or `test-engineering`.
6. Run `verification-loop`.
7. T3+: `@code-reviewer`; trust boundaries: `@security-reviewer`.
8. Pass Definition of Done; concise final report.
