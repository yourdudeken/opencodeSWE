---
description: Implement a feature with exploration, plan, tests, and verification
agent: swe
---

Feature request:

$ARGUMENTS

1. Explore relevant code (`@repo-explorer` if unfamiliar).
2. Plan minimal changes (`@planner` if multi-file / ambiguous).
3. Implement with skill `feature-implementation`.
4. Add/adjust tests via `@test-engineer` or `test-engineering`.
5. Run `verification-loop`.
6. For risky changes, `@code-reviewer` (and `@security-reviewer` if trust boundaries).
7. Concise final report with Implemented / Changed / Verified / Notes.
